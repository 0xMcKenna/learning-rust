fn main() {
    
    // Type annotated variables (define primitive)
    let n1: i64 = 100;

    let n2 = 7; // default i32

    // Mutable variables
    let mut mutable: bool = false;
    mutable = true;

    // Cannot change mutable variables type.
    // mutable = 21;
}
